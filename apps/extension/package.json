{
  "name": "kpi-schedule-sync-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Синхронізація розкладу My KPI з Telegram-ботом",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "pack": "bun run scripts/pack.ts",
    "build:zip": "vite build && bun run scripts/pack.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.308",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
